<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Electrician Services - Fix Buddy</title>
    <link rel="stylesheet" href="electrician.css">
    <link rel="stylesheet" href="toast.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto+Condensed:ital,wght@0,100..900;1,100..900&family=Rubik:ital,wght@0,300..900;1,300..900&display=swap" rel="stylesheet">
    <style>
        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.5);
            z-index: 1000;
            backdrop-filter: blur(5px);
            overflow-y: auto;
        }

        .modal-content {
            position: relative;
            background-color: #fff;
            margin: 20px auto;
            padding: 30px;
            width: 90%;
            max-width: 500px;
            border-radius: 12px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.2);
            max-height: 90vh;
            overflow-y: auto;
        }

        .close-modal {
            position: absolute;
            right: 20px;
            top: 10px;
            font-size: 28px;
            font-weight: bold;
            cursor: pointer;
            color: #666;
            z-index: 1001;
        }

        .close-modal:hover {
            color: #333;
        }

        #booking-section {
            background: none;
            box-shadow: none;
            padding: 0;
            margin: 0;
        }

        #booking-section h3 {
            color: #333;
            margin-bottom: 20px;
            font-size: 24px;
            text-align: center;
            padding-top: 10px;
        }

        #booking-form {
            display: flex;
            flex-direction: column;
            gap: 15px;
            padding-bottom: 20px;
        }

        #booking-form label {
            color: #555;
            font-size: 14px;
            font-weight: 500;
            margin-bottom: 5px;
        }

        #booking-form input,
        #booking-form textarea,
        #booking-form select {
            padding: 12px;
            border: 1px solid #ddd;
            border-radius: 6px;
            font-size: 14px;
            width: 100%;
            box-sizing: border-box;
        }

        #booking-form textarea {
            min-height: 100px;
            resize: vertical;
        }

        #booking-form select {
            background-color: white;
            cursor: pointer;
        }

        #booking-form button {
            background-color: #ffb300;
            color: white;
            padding: 12px;
            border: none;
            border-radius: 6px;
            font-size: 16px;
            font-weight: 500;
            cursor: pointer;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin-top: 10px;
        }

        #booking-form button:hover {
            background-color: #e68900;
            transform: translateY(-2px);
        }

        @media (max-height: 800px) {
            .modal-content {
                margin: 10px auto;
                max-height: 95vh;
            }
        }
    </style>
</head>
<body>
    <header class="header">
        <div class="logo">
            <h1 style="color: white;">Fix <span style="color: orange;">Buddy</span></h1>
        </div>
        <nav class="nav">
            <ul>
                <li><a href="services">Services</a></li>
                <li><a href="testimonials">Testimonials</a></li>
                <li><a href="/login">Login</a></li>
                <li><a href="/contact-us">Contact</a></li>
            </ul>
        </nav>
    </header>

    <main>
        <section class="profiles">
            <h1>Meet Our Expert Electricians</h1>
            <div class="profile-cards" id="profile-cards">
                <!-- Dynamic profile cards will be loaded here -->
            </div>
        </section>
    </main>

    <!-- Modal for Booking Form -->
    <div id="bookingModal" class="modal">
        <div class="modal-content">
            <span class="close-modal">&times;</span>
            <section id="booking-section">
                <h3>Book an Electrician</h3>
                <form id="booking-form">
                    <div class="form-group">
                        <label for="clientName">Your Name:</label>
                        <input type="text" id="clientName" required />
                    </div>

                    <div class="form-group">
                        <label for="clientEmail">Your Email:</label>
                        <input type="email" id="clientEmail" required />
                    </div>

                    <div class="form-group">
                        <label for="clientPhone">Your Phone:</label>
                        <input type="tel" id="clientPhone" required />
                    </div>

                    <div class="form-group">
                        <label for="serviceAddress">Service Address:</label>
                        <textarea id="serviceAddress" required></textarea>
                    </div>

                    <div class="form-group">
                        <label for="preferredDate">Preferred Date:</label>
                        <input type="date" id="preferredDate" required min="<%= new Date().toISOString().split('T')[0] %>" />
                    </div>

                    <div class="form-group">
                        <label for="preferredTime">Preferred Time:</label>
                        <select id="preferredTime" required>
                            <option value="">Select a time</option>
                            <option value="09:00">9:00 AM</option>
                            <option value="10:00">10:00 AM</option>
                            <option value="11:00">11:00 AM</option>
                            <option value="12:00">12:00 PM</option>
                            <option value="13:00">1:00 PM</option>
                            <option value="14:00">2:00 PM</option>
                            <option value="15:00">3:00 PM</option>
                            <option value="16:00">4:00 PM</option>
                            <option value="17:00">5:00 PM</option>
                        </select>
                    </div>

                    <div class="form-group">
                        <label for="serviceDescription">Service Description:</label>
                        <textarea id="serviceDescription" placeholder="Please describe the electrical work needed..."></textarea>
                    </div>

                    <button type="submit">Book Now</button>
                </form>
            </section>
        </div>
    </div>

    <footer>
        <p>&copy; 2025 FixBuddy - Your Trusted Service Provider</p>
    </footer>

    <script src="toast.js"></script>
    <script>
        // Get modal elements
        const modal = document.getElementById('bookingModal');
        const closeBtn = document.getElementsByClassName('close-modal')[0];
        const profileCards = document.getElementById('profile-cards');
        let selectedProfessional = null;

        // Function to show modal
        function showModal(professional) {
            selectedProfessional = professional;
            modal.style.display = 'block';
            // Update the form title with the professional's name
            document.querySelector('#booking-section h3').textContent = `Book ${professional.name}`;
        }

        // Function to hide modal
        function hideModal() {
            modal.style.display = 'none';
            selectedProfessional = null;
        }

        // Close modal when clicking the X button
        closeBtn.onclick = hideModal;

        // Close modal when clicking outside
        window.onclick = function(event) {
            if (event.target == modal) {
                hideModal();
            }
        }

        // Set minimum date for date input to today
        document.getElementById('preferredDate').min = new Date().toISOString().split('T')[0];

        // Display professionals
        function displayProfessionals() {
            const professionals = <%- JSON.stringify(professionals || []) %>;
            const error = '<%- error || "" %>';
            
            if (error) {
                profileCards.innerHTML = `
                    <div class="error-message">
                        <p>${error}</p>
                        <button onclick="window.location.reload()">Try Again</button>
                    </div>
                `;
                return;
            }

            if (!professionals || professionals.length === 0) {
                profileCards.innerHTML = `
                    <div class="no-profiles">
                        <p>No electricians available at the moment. Please check back later.</p>
                    </div>
                `;
                return;
            }

            profileCards.innerHTML = professionals.map(professional => `
                <div class="profile-card">
                    <div class="profile-image">
                        <img src="${professional.profilePicture || '/images/default.jpg'}" alt="${professional.name}">
                    </div>
                    <div class="profile-info">
                        <h3>${professional.name}</h3>
                        <p class="experience">${professional.experience} years of experience</p>
                        <p class="message">${professional.message || 'No additional information provided.'}</p>
                        <button onclick="showModal(${JSON.stringify(professional).replace(/"/g, '&quot;')})" class="book-button">Book Now</button>
                    </div>
                </div>
            `).join('');
        }

        // Call displayProfessionals when the page loads
        document.addEventListener('DOMContentLoaded', displayProfessionals);

        // Handle booking form submission
        document.getElementById('booking-form').addEventListener('submit', async function(e) {
            e.preventDefault();
            
            if (!selectedProfessional) {
                showToast('Please select a professional first');
                return;
            }

            // Disable submit button
            const submitButton = this.querySelector('button[type="submit"]');
            submitButton.disabled = true;
            submitButton.textContent = 'Submitting...';
            submitButton.style.opacity = '0.7';
            submitButton.style.cursor = 'not-allowed';

            const formData = {
                professionalId: selectedProfessional._id,
                professionalName: selectedProfessional.name,
                professionalEmail: selectedProfessional.email,
                serviceType: 'electrician',
                clientName: document.getElementById('clientName').value,
                clientEmail: document.getElementById('clientEmail').value,
                clientPhone: document.getElementById('clientPhone').value,
                serviceAddress: document.getElementById('serviceAddress').value,
                preferredDate: document.getElementById('preferredDate').value,
                preferredTime: document.getElementById('preferredTime').value,
                serviceDescription: document.getElementById('serviceDescription').value
            };

            try {
                const response = await fetch('/api/bookings', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify(formData)
                });

                const data = await response.json();

                if (response.ok) {
                    // Show success message
                    const successMessage = document.createElement('div');
                    successMessage.className = 'success-message';
                    successMessage.style.backgroundColor = '#4CAF50';
                    successMessage.style.color = 'white';
                    successMessage.style.padding = '15px';
                    successMessage.style.marginTop = '20px';
                    successMessage.style.borderRadius = '5px';
                    successMessage.style.textAlign = 'center';
                    successMessage.textContent = 'Booking successful! We will contact you shortly.';
                    
                    // Replace form with success message
                    const form = document.getElementById('booking-form');
                    form.style.display = 'none';
                    form.parentNode.insertBefore(successMessage, form.nextSibling);
                    
                    // Hide modal after 3 seconds
                    setTimeout(() => {
                        hideModal();
                        this.reset();
                        // Reset button state
                        submitButton.disabled = false;
                        submitButton.textContent = 'Book Now';
                        submitButton.style.opacity = '1';
                        submitButton.style.cursor = 'pointer';
                    }, 3000);
                } else {
                    showToast(data.error || 'Failed to book service. Please try again.');
                    // Reset button state on error
                    submitButton.disabled = false;
                    submitButton.textContent = 'Book Now';
                    submitButton.style.opacity = '1';
                    submitButton.style.cursor = 'pointer';
                }
            } catch (error) {
                console.error('Booking error:', error);
                showToast('An error occurred. Please try again later.');
                // Reset button state on error
                submitButton.disabled = false;
                submitButton.textContent = 'Book Now';
                submitButton.style.opacity = '1';
                submitButton.style.cursor = 'pointer';
            }
        });
    </script>
</body>
</html>
